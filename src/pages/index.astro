---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { getCollection } from 'astro:content';
import { BADGE_COLORS } from '../consts';

const posts = (await getCollection('blog'))
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())
  .slice(0, 4);
---

<!doctype html>
<html lang="nl">
  <head>
    <BaseHead title="Home" description="Portfolio Niels Janssen" />
    <link rel="stylesheet" href="/styles/global.css" />
  </head>
  <body>
    <Header />

    <div class="hero">
      <div class="hero-inner">
        <div class="eyebrow">Home - Project Blog</div>
        <h2 class="hero-title">Niels Janssen</h2>
        <p class="hero-subtitle">Welkom op mijn portfolio. Hier presenteer ik mijn stage, projecten en de vaardigheden die ik heb opgebouwd tijdens mijn bachelor Cloud & Cybersecurity.</p>
      </div>
    </div>

    <section class="section">
      <div class="section-label">Projecten</div>
      <h2 class="section-title">Wat ik gebouwd heb</h2>

      <div class="posts-grid">
        {posts.map((post) => (
          <a href={`/blog/${post.id}/`} class="post-card">
            <div class="post-card-top">
              <span class={`badge ${BADGE_COLORS[post.data.category] || 'badge-green'}`}>
                {post.data.category}
              </span>
            </div>
            <div class="post-card-title">{post.data.title}</div>
            <p class="post-card-desc">{post.data.description}</p>
            <div class="post-card-footer">
              <span class="post-date">
                {post.data.pubDate.toLocaleDateString('nl-BE', { year: 'numeric', month: 'long', day: 'numeric' })}
              </span>
              <span class="post-arrow">Lees meer →</span>
            </div>
          </a>
        ))}
      </div>
    </section>

    <Footer />
  </body>
</html>