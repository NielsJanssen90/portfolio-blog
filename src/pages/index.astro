---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { getCollection } from 'astro:content';

const posts = (await getCollection('blog'))
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())
  .slice(0, 3);

const colors: Record<string, string> = {
  automation:     'green',
  infrastructure: 'blue',
  security:       'red',
  reflectie:      'amber',
  algemeen:       'green',
};
---

<!doctype html>
<html lang="nl">
  <head>
    <BaseHead title="Home" description="blogtest" />
    <link rel="stylesheet" href="/styles/global.css" />
  </head>
  <body>
    <Header />

    <div class="hero">
      <div class="hero-inner">
        <div class="eyebrow">Cloud & Cybersecurity</div>
        <h5>Niels Janssen</h5>
        <p class="hero-desc">vier jaar leren over en bouwen aan veilige en betrouwbare IT-infrastructuur.ier jaar bouwen aan veilige en betrouwbare IT-infrastructuur.</p>
      </div>
    </div>

    <div class="section-label">
    <h2 class="section-title"></h2>
    </div>

    <a href="#" class="project-card">
      <div class="project-meta">
        <span class="project-category">Stage Project</span>
        <span class="project-period">16 weken</span>
      </div>
      <div class="project-title">Network Automation met Ansible & Semaphore</div>
      <p class="project-desc">blablabla</p>
      <div class="project-arrow">Lees meer →</div>
    </a>